import React from 'react';
import GoogleLogin from '../components/Login/GoogleLogin';

const Login = () => {
    return (
        <div>
            <GoogleLogin/>
        </div>
    );
};

export default Login;